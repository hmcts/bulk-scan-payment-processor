server:
  port: 8583

management:
  endpoint:
    health:
      show-details: "always"
  endpoints:
    web:
      base-path: "/"
      exposure:
        include: health, info, prometheus
  security:
    enabled: false

spring:
  application:
    name: Bulk Scan Payment Processor
